# Device Agent HIL summary

- environment: `lab-surrogate`
- counts: `{'pass': 3, 'blocked': 7}`
- minewing_claimable: **False**

Production Minewing claims require `DA_HIL_ENV=physical`, loaded profile `minewing-gw1-r1`, and zero fail/blocked rows, then `DA_HIL_SIGN=1`.
